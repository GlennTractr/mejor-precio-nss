NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_BASE_KEY= # The default key for the client (can be changed for tests. Fallback to NEXT_PUBLIC_SUPABASE_ANON_KEY)

# Typesense Configuration (Server-only for security)
# IMPORTANT: These keys are server-only and NEVER exposed to the frontend
# All Typesense operations go through secure API endpoints: /api/typesense/*
TYPESENSE_ADMIN_API_KEY=your-typesense-admin-api-key
TYPESENSE_HOST=localhost
TYPESENSE_PORT=8108
TYPESENSE_PROTOCOL=http
TYPESENSE_COLLECTION_NAME=product

# REMOVED FOR SECURITY: 
# - NEXT_PUBLIC_TYPESENSE_ADMIN_API_KEY (exposed admin credentials to frontend)
# - NEXT_PUBLIC_TYPESENSE_* (all public Typesense variables removed)
# Use /api/typesense/* endpoints instead of direct client access

# Resend Email Configuration
RESEND_API_KEY=re_your_resend_api_key_here
RESEND_FROM_EMAIL=noreply@mejorprecio.com
RESEND_TO_EMAIL=info@mejorprecio.com

# Country Configuration
NEXT_PUBLIC_COUNTRY_CODE=MX

# Currency Configuration
NEXT_PUBLIC_CURRENCY=MXN
